function make_grid(width, height) {
	grid = [];
	while (width-- > 0) {
		grid.add([null] * height);
	}
	return grid;
}

class RenderCounter {
	static field counter = 0;
}
function get_render_counter() {
	return RenderCounter.counter++;
}